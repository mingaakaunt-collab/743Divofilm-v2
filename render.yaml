services:
  - type: web
    name: kino-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python kino_bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: ADMIN_IDS
        sync: false
